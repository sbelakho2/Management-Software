# AI Change Control

No intelligence component changes without: baseline → candidate → evaluation →
shadow result → approval → version → rollback (item 100). Applies to prompt changes,
retrieval changes, context budget, model routing, tool descriptions, embedding
models, lesson extraction. The Standard Work immutability guard is the model for
controlled change in the operational layer (A8).
